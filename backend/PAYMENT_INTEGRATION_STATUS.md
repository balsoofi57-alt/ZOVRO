# Payment integration status

Stripe Connect payment service and safety checks are implemented. Production payment routes remain inactive until Render receives Stripe secret and webhook signing environment variables and provider connected-account onboarding is enabled.
